/**
 * Re-exportaciones del sistema de tema principal.
 * @see {@link ../theme.ts}
 */
export {
  BRAND_COLORS,
  DARK_THEME,
  LIGHT_THEME,
  PALETTE,
  STATUS_COLORS,
  THEME,
  THEMES,
  getBackgroundColor,
  getBorderColor,
  getCardColor,
  getCardFgColor,
  getForegroundColor,
  getInputColor,
  getRingColor,
  getStatusColors,
  getTextMutedColor,
  getTextPrimaryColor,
  getTextSecondaryColor,
  getTheme,
  getThemeColors,
  toCssCustomProperties,
  type BrandColor,
  type StatusColor,
  type ThemeColors,
  type ThemeDefinition,
  type ThemeMode,
} from "../theme";
